# TruckScan Dashboard Demo

Temporary static dashboard demo for TruckScan.

This project is intended to be deployed with GitHub Pages. It reads demo data from `data/events.json` and renders a static HTML/CSS/JS dashboard.

## Local preview

Run:

python -m http.server 8000

Then open:

http://localhost:8000
